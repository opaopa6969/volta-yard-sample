# TODO

- [ ] `parseDuration` に読めない文字列を渡すと **黙って `NaN` を返す**。
      呼ぶ側は間違いに気づけないまま計算を続けてしまう。投げるべきか、
      `null` を返して呼ぶ側に判断させるべきか決めたい。
- [ ] 週・月の単位 (`w` / `mo`) をどうするか。`m` が分なので `mo` にするしかない？
- [ ] README の例を実際に動かして確かめる仕組みが無い。
